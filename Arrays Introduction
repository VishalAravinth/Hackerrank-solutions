#include <iostream>
using namespace std;


int main() {
    int size,i,ary[1000];
    cin>>size;
    for(i=0; i<size; i++)   cin>>ary[i];
    for(i=size-1; i>0; i--)   cout<<ary[i]<<" ";
    cout<<ary[0];
    return 0;
}
